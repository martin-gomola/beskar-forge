# Claude Guide

This project's AI-agent guide is consolidated in [`AGENTS.md`](./AGENTS.md).
Claude Code reads `AGENTS.md` natively — the canonical content lives there.

This file exists only so older Claude Code versions that look for
`CLAUDE.md` first still pick up the project context.
